diff --git a/MODULE.bazel b/MODULE.bazel
index 0a5ea5b..82411c9 100755
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -12,6 +12,8 @@ module(
 # Bazel module dependencies
 # =========================================
 
+bazel_dep(name = "bazel_features", version = "1.11.0")
+
 bazel_dep(name = "rules_license", version = "0.0.7")
 bazel_dep(name = "bazel_skylib", version = "1.7.1")
 bazel_dep(name = "protobuf", version = "21.7", repo_name = "com_google_protobuf")
@@ -41,6 +43,10 @@ single_version_override(
     patch_strip = 1,
     patches = ["//third_party:rules_jvm_external_6.0.patch"],
 )
+single_version_override(
+    module_name = "bazel_features",
+    version = "1.11.0",
+)
 
 local_path_override(
     module_name = "remoteapis",
